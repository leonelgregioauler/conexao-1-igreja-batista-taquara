import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Conexão 1º Igreja Batista Taquara',
  webDir: 'www'
};

export default config;
